import AdminSidebar from "../component/AdminSidebar";

const Products = () => {
  return (
    <div className="adminContainer">
      {/* Sidebar */}
      <AdminSidebar />
      <main>Product</main>
    </div>
  );
};

export default Products;
